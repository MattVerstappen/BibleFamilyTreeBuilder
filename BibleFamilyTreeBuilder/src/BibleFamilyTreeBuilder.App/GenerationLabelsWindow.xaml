<Window x:Class="BibleFamilyTreeBuilder.App.GenerationLabelsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Generations"
        Height="560"
        Width="820"
        MinHeight="460"
        MinWidth="720"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Window}"
        TextElement.Foreground="{DynamicResource Brush.TextPrimary}"
        TextElement.FontFamily="Segoe UI">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Manage Generations"
                       FontSize="22"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource Brush.TextPrimary}" />
            <TextBlock Text="Add, edit, or delete row labels. This only changes generation titles and notes, not people or manual generation overrides."
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource Brush.TextSecondary}"
                       Margin="0,4,0,0" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="260" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{DynamicResource Brush.Panel}"
                    BorderBrush="{DynamicResource Brush.PanelBorder}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="8"
                    Margin="0,0,12,0">
                <DataGrid x:Name="LabelsGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionChanged="LabelsGrid_SelectionChanged"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          Background="{DynamicResource Brush.Panel}"
                          BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Generation" Binding="{Binding GenerationNumber}" Width="90" />
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="160" />
                        <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*" />
                        <DataGridTextColumn Header="People" Binding="{Binding PeopleCount}" Width="70" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="1"
                    Background="{DynamicResource Brush.Panel}"
                    BorderBrush="{DynamicResource Brush.PanelBorder}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="12">
                <StackPanel>
                    <TextBlock Text="Generation number" />
                    <TextBox x:Name="GenerationNumberBox"
                             Margin="0,2,0,10" />

                    <TextBlock Text="Title" />
                    <TextBox x:Name="TitleBox"
                             Margin="0,2,0,10" />

                    <TextBlock Text="Notes" />
                    <TextBox x:Name="NotesBox"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             Height="130"
                             Margin="0,2,0,14" />

                    <Button Content="Add Label"
                            Click="AddLabel_Click"
                            Margin="0,0,0,8" />
                    <Button Content="Save Changes"
                            Click="SaveChanges_Click"
                            Margin="0,0,0,8" />
                    <Button Content="Delete Selected Label"
                            Click="DeleteSelectedLabel_Click"
                            Margin="0,0,0,8" />
                    <Button Content="Clear Form"
                            Click="ClearForm_Click" />
                </StackPanel>
            </Border>
        </Grid>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Close"
                    Width="100"
                    IsCancel="True"
                    Click="Close_Click" />
        </StackPanel>
    </Grid>
</Window>
