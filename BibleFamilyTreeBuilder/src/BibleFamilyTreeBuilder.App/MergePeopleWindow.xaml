<Window x:Class="BibleFamilyTreeBuilder.App.MergePeopleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Merge People"
        Height="620"
        Width="760"
        MinHeight="520"
        MinWidth="680"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Window}"
        TextElement.Foreground="{DynamicResource Brush.TextPrimary}"
        TextElement.FontFamily="Segoe UI">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Merge People"
                       FontSize="22"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource Brush.TextPrimary}" />
            <TextBlock Text="Keep the primary person and merge the secondary person's names, references, notes, variants, and relationships into it."
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource Brush.TextSecondary}"
                       Margin="0,4,0,0" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                <TextBlock Text="Primary person to keep" />
                <ComboBox x:Name="PrimaryPersonCombo"
                          DisplayMemberPath="EffectiveDisplayName"
                          SelectionChanged="PersonSelectionChanged"
                          Margin="0,3,0,0" />
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                <TextBlock Text="Secondary person to merge" />
                <ComboBox x:Name="SecondaryPersonCombo"
                          DisplayMemberPath="EffectiveDisplayName"
                          SelectionChanged="PersonSelectionChanged"
                          Margin="0,3,0,0" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="2"
                Background="{DynamicResource Brush.Panel}"
                BorderBrush="{DynamicResource Brush.PanelBorder}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="10">
            <TextBox x:Name="PreviewBox"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     BorderThickness="0"
                     Background="{DynamicResource Brush.Panel}"
                     FontFamily="Consolas"
                     FontSize="12" />
        </Border>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Merge People"
                    Width="120"
                    Margin="0,0,8,0"
                    Click="MergePeople_Click" />
            <Button Content="Cancel"
                    Width="100"
                    IsCancel="True"
                    Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
