<UserControl x:Class="BibleFamilyTreeBuilder.App.Controls.TreeCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Brush.Canvas}">
    <Grid ClipToBounds="True">
        <Canvas x:Name="PART_Canvas"
                Width="1400"
                Height="900"
                Background="{DynamicResource Brush.Canvas}"
                MouseDown="Canvas_MouseDown"
                MouseMove="Canvas_MouseMove"
                MouseUp="Canvas_MouseUp"
                MouseWheel="Canvas_MouseWheel">
            <Canvas.RenderTransform>
                <TransformGroup>
                    <ScaleTransform x:Name="ScaleTransform" ScaleX="1" ScaleY="1" />
                    <TranslateTransform x:Name="PanTransform" X="0" Y="0" />
                </TransformGroup>
            </Canvas.RenderTransform>
        </Canvas>

        <Border HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="16"
                Padding="12"
                Background="{DynamicResource Brush.Panel}"
                BorderBrush="{DynamicResource Brush.PanelBorder}"
                BorderThickness="1"
                CornerRadius="8"
                Opacity="0.97">
            <Border.Effect>
                <DropShadowEffect BlurRadius="16"
                                  ShadowDepth="2"
                                  Opacity="0.16"
                                  Color="#443A2F" />
            </Border.Effect>
            <StackPanel Width="250">
                <TextBlock Text="Legend"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource Brush.TextPrimary}"
                           Margin="0,0,0,8" />
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <Rectangle Width="18" Height="12" Fill="{DynamicResource Brush.CardDefaultFill}" Stroke="{DynamicResource Brush.CardDefaultBorder}" RadiusX="3" RadiusY="3" />
                    <TextBlock Text="Green = Default" Margin="8,0,0,0" Foreground="{DynamicResource Brush.TextPrimary}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <Rectangle Width="18" Height="12" Fill="{DynamicResource Brush.CardJesusFill}" Stroke="{DynamicResource Brush.CardJesusBorder}" RadiusX="3" RadiusY="3" />
                    <TextBlock Text="Yellow = Jesus Line" Margin="8,0,0,0" Foreground="{DynamicResource Brush.TextPrimary}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <Rectangle Width="18" Height="12" Fill="{DynamicResource Brush.CardUnknownDescFill}" Stroke="{DynamicResource Brush.CardUnknownDescBorder}" RadiusX="3" RadiusY="3" />
                    <TextBlock Text="Blue = Unknown descendants" Margin="8,0,0,0" Foreground="{DynamicResource Brush.TextPrimary}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <Line X1="0" Y1="7" X2="22" Y2="7" Stroke="{DynamicResource Brush.MarriageLine}" StrokeThickness="3" StrokeDashArray="6,3" />
                    <TextBlock Text="Red/pink = Marriage" Margin="8,0,0,0" Foreground="{DynamicResource Brush.TextPrimary}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Line X1="0" Y1="7" X2="22" Y2="7" Stroke="{DynamicResource Brush.AdoptedLegalLine}" StrokeThickness="2.5" StrokeDashArray="4,3" />
                    <TextBlock Text="Dashed = Adopted/Legal parent" Margin="8,0,0,0" Foreground="{DynamicResource Brush.TextPrimary}" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
